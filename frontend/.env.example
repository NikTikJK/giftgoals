# URL бэкенд-API (используется в коде через import.meta.env.VITE_API_BASE_URL).
# Локально: прокси Vite перенаправляет /api на бэкенд, можно оставить /api.
# Production (Render): полный URL бэкенда с путём /api, напр. https://giftgoals-backend.onrender.com/api
VITE_API_BASE_URL=http://localhost:3001/api
